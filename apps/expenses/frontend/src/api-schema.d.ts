// Generated by `bun run gen:client` from openapi/expenses.yaml — do not edit.
// CI regenerates this file and fails when it is stale.

export interface paths {
    "/api/v1/expenses/approvals": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the approval queue
         * @description The approval queue — the submitted *units* the caller may approve (every one of them for expenses:approve, the ones on the projects they manage otherwise, and not those dated before the period lock unless they hold expenses:manage, whose approval the lock would refuse anyway) — grouped per person, the person who has been waiting longest first, and paged by person, so a page always holds whole groups. A unit is a standalone expense or a whole travel claim: each group carries its entries and its claims separately, and a claim's lines are never listed as loose expenses.
         */
        get: operations["getExpensesApprovals"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/approve": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Approve expenses
         * @description Approves submitted units — standalone expenses, travel claims, or both in one batch (decision X4) — recording who decided and when. Each must be submitted, each must be one the caller approves for (expenses:approve for anything, or the manager role on its project; a claim is judged on the claim's own project), and none may be dated before the period lock unless the caller holds expenses:manage — a claim on the day it departed. The frozen amounts are not touched: an approval agrees with them.
         */
        post: operations["postExpensesApprove"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/attachments/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Download a receipt
         * @description Streams a receipt's bytes with the type they were stored under, named as they were uploaded (RFC 5987), never cached and never sniffed. Visibility is the expense's own: anyone who may not see it gets the same bare 404 an unknown id gets.
         */
        get: operations["getExpensesAttachmentsById"];
        put?: never;
        post?: never;
        /**
         * Delete a receipt
         * @description Removes a receipt while its expense is still its owner's to change — a draft or a rejected line, by its owner or by expenses:manage, and not before the period lock. The stored object goes with the row. The two refusals are told apart as they are everywhere else here: who is asking is a 403, what the expense is right now is a 400 naming the reason.
         */
        delete: operations["deleteExpensesAttachmentsById"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/categories": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List expense categories
         * @description Every expense category in its own order, inactive ones included so a stored expense can still name its own. Positions are a dense 1..n that the server keeps, so this order is the positions themselves.
         */
        get: operations["getExpensesCategories"];
        put?: never;
        /**
         * Add an expense category
         * @description Adds an expense category. With no position it goes last; with one it is inserted at that place and the rest of the list is renumbered around it.
         */
        post: operations["postExpensesCategories"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/categories/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Change an expense category
         * @description Replaces a category's name, whether it may be chosen, and where it sits. A category is never deleted — one that is no longer wanted is deactivated, so the expenses booked on it keep their name. Moving one is this operation: send the place it should take and the server takes it out of the list, puts it back there and renumbers every category 1..n, so a move always moves. A request that repeats the place the category already holds reorders nothing.
         */
        put: operations["putExpensesCategoriesById"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/claims": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List travel claims
         * @description Travel claims, the most recent trip first, and only where the caller may see them — their own, everything on a project they manage, and everyone's for expenses:view-all, expenses:approve and expenses:manage. The same rule a single read applies, so the list never holds a claim its own read would answer 404 for.
         */
        get: operations["getExpensesClaims"];
        put?: never;
        /**
         * Record a travel claim
         * @description Records one travel claim as a draft with no lines. Expenses are added to it afterwards, with POST /entries carrying its claimId.
         */
        post: operations["postExpensesClaims"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/claims/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a travel claim by id
         * @description One travel claim with its lines, shaped for the caller. A claim the caller may not see answers the same bare 404 an unknown id does.
         */
        get: operations["getExpensesClaimsById"];
        /**
         * Change a travel claim
         * @description Replaces a travel claim's header, guarded by the revision it was read at. Changing its project re-points every line in the same transaction. The trip's own fields answer for the per diem days it already holds, in that same transaction: narrowing departureAt or returnAt past a day already recorded is refused naming the stranded dates — a day somebody recorded is theirs to remove — and changing abroad, abroadDayRate or abroadCurrency reprices every per diem day of the claim, refusing the whole edit if one of them could no longer be priced at all.
         */
        put: operations["putExpensesClaimsById"];
        post?: never;
        /**
         * Delete a travel claim
         * @description Deletes a travel claim while it is still its owner's to change — a draft or a rejected one, by its owner or by expenses:manage, and not before the period lock. Its lines go with it, and their receipts with them. The two refusals are told apart on purpose: who is asking is a 403, what the claim is right now is a 400 naming the reason.
         */
        delete: operations["deleteExpensesClaimsById"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/claims/{id}/per-diem-suggestion": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Suggest a trip's per diem days
         * @description The per diem days a trip would have, worked out from its departure and its return (design §4). It writes nothing: the client shows the days and records whichever ones the traveller agrees with as ordinary per diem lines. A trip under six hours suggests none. Without an overnight it is one day — day_6_12 up to and including twelve hours, day_over_12 beyond — dated on the departure day. With one it is one overnight_hotel line per full 24-hour period from the departure, plus one more when what is left over is strictly longer than six hours — and never fewer than one, so any overnight trip from six hours up is a day. The six hours is therefore inclusive as the threshold a whole trip has to clear and exclusive for the remainder after a full period: six hours exactly with an overnight is one day, thirty hours exactly is still one, and thirty hours and a minute is two. The day boundaries are 24 hours from the departure instant, not calendar midnights; the dates are consecutive calendar days from the departure's own day in the installation's business time zone, so the two nights a year the clocks move cannot put two days on one date or skip one. It is a POST because the answer depends on a body, and readable by whoever may see the claim.
         */
        post: operations["postExpensesClaimsByIdPerDiemSuggestion"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List expenses
         * @description Expenses, the latest day first, and only where the caller may see them — their own, everything on a project they manage, everyone's for expenses:view-all, expenses:approve and expenses:manage, and a supplier invoice to anyone with financial rights on its project. The same rule a single read applies, so the list never holds an entry its own read would answer 404 for and its total counts only what the caller may see.
         */
        get: operations["getExpensesEntries"];
        put?: never;
        /**
         * Record an expense
         * @description Records one expense as a draft. A mileage line and a per diem day are priced from the dated rate table for the entry's own date; an outlay is entered as it stands.
         */
        post: operations["postExpensesEntries"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get an expense by id
         * @description One expense, shaped for the caller. An expense the caller may not see answers the same bare 404 an unknown id does.
         */
        get: operations["getExpensesEntriesById"];
        /**
         * Change an expense
         * @description Replaces an expense, guarded by the revision it was read at. A mileage line is priced again from the rate table, so a draft always shows what it is worth today.
         */
        put: operations["putExpensesEntriesById"];
        post?: never;
        /**
         * Delete an expense
         * @description Deletes an expense while it is still its owner's to change — a draft or a rejected line, by its owner or by expenses:manage, and not before the period lock. The two refusals are told apart on purpose: who is asking is a 403, what the expense is right now is a 400 naming the reason.
         */
        delete: operations["deleteExpensesEntriesById"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/attachments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Attach a receipt to an expense
         * @description Attaches one receipt to an outlay, or the supplier's invoice to a supplier invoice: a JPEG, PNG, HEIC or PDF of at most 10 MB, and at most ten per expense. The type is read from the file's own bytes and has to agree with what the client called it. Only whoever may change the expense may attach to it, and only while it is a draft or rejected.
         */
        post: operations["postExpensesEntriesByIdAttachments"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/billing-lines": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the billing lines an expense may be priced against
         * @description The billing lines of the expense's own project, for the caller who prices it. GET /api/v1/expenses/projects cannot serve the pricing dialog: it answers the projects the *caller* may book an expense on, which is the member-or-manager right on a project still open for work, while pricing belongs to whoever may see the project's money — its manager, projects:manage-all, or projects:view-financials on a project they can see. A finance person on no project team, and anybody pricing a line on a project that has been completed, would be offered nothing at all by that list. This one is keyed on the expense instead and judged by exactly the rule PUT /entries/{id}/billing is judged by, so the dialog can never offer a line the save then refuses. Active lines, ordered by code, plus the expense's own current line even if the project has since stopped using it.
         */
        get: operations["getExpensesEntriesByIdBillingLines"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/billing": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Price an expense from its project
         * @description Prices one expense from its project's side: what it bills the customer, and which billing line it is booked against. This is a door of its own because expenses:manage does not imply financial rights on a project and an employee's expense form never carries a markup — the project's manager prices the line when they approve it, whatever status it has reached. It changes nothing else about the expense.
         */
        put: operations["putExpensesEntriesByIdBilling"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/invoiced": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark an expense invoiced
         * @description Marks one approved, billable line as billed on to the customer (decision X5). The caller needs financial rights on the line's project — its managers, projects:manage-all, or projects:view-financials on a project they can see — and expenses:manage is not it: paying the employee and charging the customer are different jobs. The line must be approved, billable and carry an amount to bill: a billable mileage line saved while no customer rate was in force carries none and has to be priced first. The period lock does not hold it back — the lock protects what the employee submitted and what was approved, and invoicing is bookkeeping done after the period closes. The two tracks are independent: a line that has been reimbursed is still invoiceable, and the other way round.
         */
        post: operations["postExpensesEntriesByIdInvoiced"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/invoiced/undo": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Undo marking an expense invoiced
         * @description Takes the invoicing back off one line, under exactly the rights that set it and with the same revision guard.
         */
        post: operations["postExpensesEntriesByIdInvoicedUndo"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/entries/{id}/rate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Override a rate
         * @description Replaces the rate on one submitted mileage line or per diem day (decision X8) and reprices its amount. Its owner cannot — unless they also approve it, because self-approval is allowed. What the rate table had said is recorded beside the new rate, the first time and no later, so the original is never lost.
         */
        put: operations["putExpensesEntriesByIdRate"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/meta": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the Expenses metadata
         * @description What this installation of Expenses can do (decision X2) and the settings and categories every form needs, in one read.
         */
        get: operations["getExpensesMeta"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/projects": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the projects an expense may be booked on
         * @description The projects the caller may book an expense on, with their active billing lines, so the expense form needs no code of the projects module's own. With userId it is the projects that person may book on instead, which is what a form recording an expense for somebody else needs: the save is judged on the owner's own bookability, so a picker offering the caller's would offer projects the save then refuses. Answers 404 in an installation with no projects module (decision X2) — the same answer as a path that is not there.
         */
        get: operations["getExpensesProjects"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/projects/{projectId}/summary": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Sum up a project's expenses
         * @description What one project's expenses cost and bill, in sum — the Expenses tab on the project page. It is for whoever has **financial rights on the project**: its manager, projects:manage-all, or projects:view-financials on a project they can see. expenses:manage, expenses:approve and expenses:view-all do not grant it — they open other people's expenses, not a project's money, which is the same rule pricing a line is held to. Everybody else, an unknown project and an installation with no projects module are one and the same **bare 404**, so nothing here says whether a project exists. The figures are aggregates only: they do not widen who may see an individual expense, so a reader who is not the project's manager may well see these totals over a list (GET /entries?projectId=) that answers them nothing. Everything is per currency and **nothing is converted**; a rejected expense counts as draft.
         */
        get: operations["getExpensesProjectsByProjectIdSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/rates": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the expense rates
         * @description Every dated rate, by kind and then the latest first. The client groups them per kind. A caller without expenses:manage reads every kind except mileage_customer — what the company charges its customer per kilometre is a commercial price — so an expense form can preview what a mileage line will pay without an administrator's rights.
         */
        get: operations["getExpensesRates"];
        put?: never;
        /**
         * Add an expense rate
         * @description Adds a dated rate. It prices expenses saved from now on; a submitted expense's rate never moves.
         */
        post: operations["postExpensesRates"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/rates/reset": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Restore a rate kind's seeded rows
         * @description Puts one kind back to what it shipped with (decision X8): a shipped day that was removed is restored, and one that was edited returns to its shipped value, currency and source label. The company's own rows, on days the product never shipped, are untouched, and nothing is ever removed. A kind with no shipped rows changes nothing.
         */
        post: operations["postExpensesRatesReset"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/rates/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Change an expense rate
         * @description Replaces a rate row's day, value, currency and source. The kind stays the row's own.
         */
        put: operations["putExpensesRatesById"];
        post?: never;
        /**
         * Delete an expense rate
         * @description Removes a rate row. Expenses it priced keep what was snapshotted on them.
         */
        delete: operations["deleteExpensesRatesById"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/reimbursed": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark expenses reimbursed
         * @description Marks approved units as paid back to the people who are owed them (decision X5) — one payroll run over standalone expenses, travel claims, or both, recorded with the day it was made and the reference whoever made it can find it by. A trip is one unit, paid for the sum of what its lines owe its owner. All or nothing: a unit that is not approved, owes the employee nothing, or has been paid already refuses the whole request on the list that named it and nothing is stamped. The period lock does not hold it back — payroll runs after the books close, and only expenses:manage, whom the lock never held back anyway, may do this at all.
         */
        post: operations["postExpensesReimbursed"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/reimbursed/undo": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Undo marking expenses reimbursed
         * @description Takes the reimbursement stamp back off units that were paid by mistake — the whole of it, so they appear in the waiting list again exactly as they were. A travel claim's stamp is the claim's own, so undoing it puts the whole trip back.
         */
        post: operations["postExpensesReimbursedUndo"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/reimbursements": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List what is owed back
         * @description What a payroll run is made from: approved *units* that owe their owner something and have not been paid — standalone expenses and whole travel claims — grouped per person with their totals per currency, and paged by person, so a page always holds whole people. A trip is one unit, owed the sum of what its lines owe; its lines are never listed as loose expenses. state=reimbursed answers what has already been paid instead, the latest payout first, so an undo is reachable.
         */
        get: operations["getExpensesReimbursements"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/reimbursements/export.csv": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Export what is owed back as CSV
         * @description The same expenses the reimbursement list holds, as the file a payroll system reads: UTF-8 with a byte order mark, semicolon-separated, decimal comma, ISO dates, CRLF line ends and a header row — the form a Norwegian Excel opens correctly without an import dialog. Quoting is RFC 4180's with the semicolon as the separator: a value holding a semicolon, a quote or a line break is quoted and its quotes doubled. A text cell beginning with '=', '+', '-', '@', a tab or a carriage return is prefixed with an apostrophe, so a description nobody vetted cannot become a formula in somebody's spreadsheet.
         *
         *     The columns, in order: unit, purpose, employee display name, user id, date, kind, description, category, currency, gross, VAT, owed, project code. One row per *line*: a standalone expense is its own line, and a travel claim writes one row per expense it holds. The unit cell says which — 'expense 2001' or 'claim 1012' — and purpose carries the trip's own, empty for a standalone expense. A per diem day has no description of its own, so its description cell carries the per diem type and its category cell is empty. Rows are ordered by the person's display name, then by the unit's own day — a standalone expense's date, a travel claim's departure day in the installation's time zone — then by the unit itself, and inside a unit by the line's date and id: a trip's lines stand together under it rather than being split by a loose expense dated between them. The VAT and project code cells are empty when there is none — including in an installation with no projects module, which keeps the column so a payroll system need not know which modules an installation runs.
         *
         *     It takes the list's own filters, or explicit entryIds and claimIds instead of them, and it is deliberately not paged: half a payroll file is worse than none, so an export of more than 5000 rows is refused and asks for a narrower filter. The cap counts rows, so a trip of forty lines costs forty of them.
         */
        get: operations["getExpensesReimbursementsExportCsv"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/reject": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reject expenses
         * @description Rejects submitted units — standalone expenses, travel claims, or both — with a reason their owner sees, under exactly the rules an approval follows. A rejected unit is its owner's to edit, delete or submit again; submitting it prices it once more, so a line sent back over a wrong rate comes back at the right one. A claim's rejection is stamped on the claim, and its lines carry no decision of their own.
         */
        post: operations["postExpensesReject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/settings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the expense settings
         * @description The settings every expenses:access holder may know — the currency a new expense starts in, the default markup, the receipt threshold and the period lock.
         */
        get: operations["getExpensesSettings"];
        /**
         * Change the expense settings
         * @description Replaces the installation's expense settings. Any lock date is accepted, one in the future included — the lock is an administrator's statement about which period is closed.
         */
        put: operations["putExpensesSettings"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/stats": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the caller's expense key figures
         * @description The caller's own key figures — how many of their expenses stand in each status, what they are still owed per currency, and how many expenses are waiting for them to approve.
         */
        get: operations["getExpensesStats"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/stats/attention": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get expenses dashboard attention items
         * @description The caller's own rejected expenses, the people whose expenses are waiting for them to approve, and — for expenses:manage — whether anything is waiting to be paid.
         */
        get: operations["getExpensesStatsAttention"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/stats/summary": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get expenses dashboard summary */
        get: operations["getExpensesStatsSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/stats/timeseries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get expenses dashboard time series
         * @description The caller's own approved expenses per entry date inside the period, as the net amount — gross less VAT — in the installation's default currency. It is the caller's own for the reason the hours in time's are: a figure whose scope changed with the reader's permissions would mean a different thing to every reader. Expenses in another currency are left out rather than added to a total that would be in neither. The one metric is 'netAmount'.
         */
        get: operations["getExpensesStatsTimeseries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/submit": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit expenses
         * @description Submits units for approval (decision X4): standalone expenses, travel claims, or both. Each must be the caller's own draft or rejected unit — expenses:manage submits on somebody else's behalf — and none may be dated before the period lock unless the caller holds expenses:manage, a claim on the day it departed. This is where the figures freeze: the mileage rate, the passenger supplement, a per diem day's day rate and meal percentages, the amounts they make and what the customer is billed are computed one last time from the tables in force on each line's own date, stored, and never computed again. Submitting a claim freezes every one of its lines, in one transaction: a claim with no lines at all, a line whose rate has vanished from the table, a per diem day left outside the trip by a change of the installation's time zone, or an employee-paid outlay over the receipt threshold with no receipt each refuse the whole submit and name what is wrong; a supplier invoice with no document attached refuses it as "Expense N cannot be submitted yet: Attach the supplier's invoice".
         */
        post: operations["postExpensesSubmit"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/expenses/unapprove": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Unapprove expenses
         * @description Returns approved units to a draft their owner can change and submit again — by whoever could have approved them, or by expenses:manage — clearing the decision and the submission stamp alike. For a travel claim every line's rate-override audit goes with them, so the trip is a fresh draft in every sense. The figures the submit froze stay until the next save or submit recomputes them. Never a unit that has been reimbursed, nor one whose lines have been invoiced: the reimbursement track and the invoicing track each have an undo of their own.
         */
        post: operations["postExpensesUnapprove"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description One person's submitted units that the caller may approve, with the figures an approver decides on at a glance. A unit is a standalone expense or a whole travel claim, so the group holds two lists: a claim is one row of claims and never a run of loose entries. */
        ExpensesApprovalGroup: {
            /** @description The person's submitted travel claims, the earliest departure first. Each is one unit to approve; the trip's own page (GET /claims/{id}) holds its lines. */
            claims: components["schemas"]["ExpensesClaimSummary"][];
            /** @description The person's submitted standalone expenses, oldest day first and then as recorded. Shaped exactly as a single read of each would be for this caller. A travel claim's lines are never here — the claim is the unit. */
            entries: components["schemas"]["ExpensesEntryResponse"][];
            /**
             * Format: int32
             * @description How many lines in the group carry a rate an approver has already replaced, the claims' lines included.
             */
            overriddenRates: number;
            /**
             * Format: int32
             * @description How many lines in the group are outlays with no receipt at all, the claims' lines included. Mileage and a per diem day never take one and are never counted.
             */
            receiptsMissing: number;
            /** @description The group's figures, one line per currency, by currency code — the standalone expenses and the claims' lines together. Nothing is ever converted. */
            totals: components["schemas"]["ExpensesCurrencyTotal"][];
            user: components["schemas"]["ExpensesUserRef"];
        };
        /** @description One receipt on an expense (design §3.2). The bytes themselves are never in a JSON body: they are read back from GET /attachments/{id}, which serves them with the expense's own visibility. */
        ExpensesAttachmentResponse: {
            /** @description What the receipt was detected to be when it was uploaded — image/jpeg, image/png, image/heic or application/pdf — and what the download is served as. */
            contentType: string;
            /** @description The name the client uploaded it under, stripped of any path and capped at 255 characters. It is never what the object is stored under. */
            fileName: string;
            /** Format: int64 */
            id: number;
            /**
             * Format: int64
             * @description The receipt's size in bytes, at most 10 MB.
             */
            sizeBytes: number;
        };
        /** @description One billing line a pricer may book an expense against. It carries active because the list holds the expense's own current line even after the project has stopped using it: the dialog has to be able to show what is stored without offering it again. */
        ExpensesBillingLineOption: {
            /** @description Whether the project still offers the line. False only for the line this expense already carries — every other line in the list is active. */
            active: boolean;
            code: string;
            /** Format: int32 */
            id: number;
        };
        /** @description What an expense bills its customer, set from the project's side (decision X7). It is a full replace of the billing fields alone and touches nothing else about the expense: not its amount, not its status, not its receipts. Only a caller with financial rights on the entry's project may send it — expenses:manage is not one of them — because the markup and the customer rate per kilometre are the project's figures and an employee's form never carries them. Allowed while the expense is a draft, rejected, submitted or approved; refused once it has been invoiced, and refused outright on a per diem day, which is never billed on to a customer. The period lock does not reach it: the lock protects what the employee submitted and what was approved, while pricing is bookkeeping done after a period closes — an invoice for December goes out in January. */
        ExpensesBillingRequest: {
            /**
             * Format: double
             * @description What the customer is charged per kilometre. Only on billable mileage. Left out, the line keeps whatever it carries, and a line that carries none takes the mileage_customer rate in force on the entry date. Refused on a project that bills nothing, rather than accepted and cleared.
             */
            billRatePerKm?: number;
            /** @description Whether the line is billed on to the customer. Forced false on a project that bills nothing. Turning it off clears the markup, the customer rate and the bill amount. */
            billable: boolean;
            /**
             * Format: int32
             * @description An active billing line of the entry's own project, or absent to book it against none. Refused on a per diem day, which bills nobody anything.
             */
            billingLineId?: number;
            /**
             * Format: double
             * @description A billable outlay's or supplier invoice's markup on the net, 0 to 1000 with at most two decimals. Left out, the line keeps whatever it carries, and a line that carries none takes the settings' default. Refused on a project that bills nothing, rather than accepted and cleared.
             */
            markupPercent?: number;
            /**
             * Format: int32
             * @description The revision the expense was read at. A revision that has moved on is a 409.
             */
            revision: number;
        };
        /** @description A new expense category. The name is unique case-insensitively; position is the place it takes in the picker and defaults to the end. */
        ExpensesCategoryRequest: {
            /** @description Whether the category may be chosen. Absent means active. */
            active?: boolean;
            name: string;
            /**
             * Format: int32
             * @description The 1-based place the category takes in the picker; the server renumbers the others around it, so the list stays a dense 1..n. A place past the end of the list is the end. Absent puts it last.
             */
            position?: number;
        };
        /** @description One expense category (design §3.3). Categories are deactivated, never deleted, once an expense has used one. */
        ExpensesCategoryResponse: {
            active: boolean;
            /** Format: int32 */
            id: number;
            name: string;
            /**
             * Format: int32
             * @description The 1-based place it holds in the picker. Positions are a dense 1..n over every category, active or not, and the list is returned in that order — so this is the place it ended up in, which is not always the one a request asked for.
             */
            position: number;
        };
        /** @description A full replace of a category's name, whether it may be chosen, and where it sits. Renaming it onto another category's name — however it is cased — is refused on the name field. */
        ExpensesCategoryUpdateRequest: {
            active: boolean;
            name: string;
            /**
             * Format: int32
             * @description The 1-based place the category is to take in the picker; the server renumbers the others, so the list stays a dense 1..n. Send the place you want it in, not the neighbour's number: sending a place it already holds changes no order at all, and a place past the end of the list is the end. Deactivated categories keep their place and are counted in the numbering.
             */
            position: number;
        };
        /** @description What the calling user may do with this travel claim, so a client never re-derives the rules of design §5. A claim is the unit that moves through the flow and through a payroll run, so these are the claim's own — its lines carry no submit, approve or reimburse of their own. */
        ExpensesClaimCapabilities: {
            /** @description Whether the caller may approve or reject it — an approver of it, while it is submitted. */
            canApprove: boolean;
            /** @description Whether the caller may delete it, which takes its lines and their receipts with it. */
            canDelete: boolean;
            /** @description Whether the caller may change it — its owner or expenses:manage, while it is a draft or rejected, and not before the period lock. It is the same answer for adding, changing or deleting one of its lines. */
            canEdit: boolean;
            /** @description Whether the caller may mark it reimbursed — expenses:manage, while it is approved, owes the employee something and has not been paid yet. The period lock does not hold it back: payroll runs after the books close. */
            canMarkReimbursed: boolean;
            /** @description Whether the caller may submit it — its owner or expenses:manage, while it is a draft or rejected, and not before the period lock. */
            canSubmit: boolean;
            /** @description Whether the caller may return it to a draft — an approver of it or expenses:manage, while it is approved and has been neither reimbursed nor left holding a line that has been invoiced. */
            canUnapprove: boolean;
            /** @description Whether the caller may take the reimbursement back — expenses:manage, while it stands reimbursed. */
            canUndoReimbursed: boolean;
        };
        /** @description One travel claim in a list — everything a single read answers except its lines, which a list of trips has no use for, plus how many there are. */
        ExpensesClaimListResponse: {
            abroad: boolean;
            /** @description The currency the claim's own day rate is in. Present only on a claim abroad. */
            abroadCurrency?: string;
            /**
             * Format: double
             * @description The day rate a trip abroad is paid at, in place of the dated table's. Present only on a claim abroad.
             */
            abroadDayRate?: number;
            capabilities: components["schemas"]["ExpensesClaimCapabilities"];
            /** Format: date-time */
            createdAt: string;
            decision?: components["schemas"]["ExpensesEntryDecision"];
            /** Format: date-time */
            departureAt: string;
            destination?: string;
            /** Format: int64 */
            id: number;
            /**
             * Format: int32
             * @description How many expenses the claim holds.
             */
            lineCount: number;
            owner: components["schemas"]["ExpensesUserRef"];
            project?: components["schemas"]["ExpensesEntryProject"];
            purpose: string;
            /** @description That the employee has been paid back for the trip. Absent until then. */
            reimbursement?: components["schemas"]["ExpensesEntryReimbursement"];
            /** Format: date-time */
            returnAt: string;
            /** Format: int32 */
            revision: number;
            /** @description 'draft', 'submitted', 'approved' or 'rejected'. */
            status: string;
            /** Format: date-time */
            submittedAt?: string;
            /** @description The claim's figures, one line per currency, by currency code. Nothing is ever converted. */
            totals: components["schemas"]["ExpensesCurrencyTotal"][];
            /** Format: date-time */
            updatedAt: string;
        };
        /** @description A travel claim (design §3.6): the container a trip's expenses sit in. Its lines are ordinary expenses carrying its id — they take their owner, their project, their status and their decision from it, and keep their own kind, date, amounts and receipts. A claim is created as a draft with no lines. */
        ExpensesClaimRequest: {
            /** @description Whether the trip was abroad. A trip abroad is paid at the claim's own day rate rather than the dated per diem table, so abroadDayRate and abroadCurrency are required with it and refused without it. */
            abroad?: boolean;
            /** @description A three-letter ISO 4217 code. Required when abroad, refused otherwise. */
            abroadCurrency?: string;
            /**
             * Format: double
             * @description The day rate the trip is paid at, greater than zero with at most two decimals. Required when abroad, refused otherwise.
             */
            abroadDayRate?: number;
            /**
             * Format: date-time
             * @description When the traveller left, as entered. It is stored and compared as the instant it names; the per diem day boundaries are 24-hour periods from it, not calendar midnights.
             */
            departureAt: string;
            /** @description At most 200 characters. */
            destination?: string;
            /**
             * Format: int32
             * @description The project the whole trip is booked on, and so every one of its lines. The person it concerns must be allowed to book on it — what logging time needs. Refused when this installation has no projects module.
             */
            projectId?: number;
            /** @description What the trip was for. 1 to 200 characters. */
            purpose: string;
            /**
             * Format: date-time
             * @description When the traveller came back. It must be after the departure, and the trip may be at most 366 days.
             */
            returnAt: string;
            /**
             * Format: uuid
             * @description The person the claim concerns. Absent means the caller; naming somebody else needs expenses:manage, and they must be a user identity still has as active.
             */
            userId?: string;
        };
        /** @description One travel claim as the caller may see it, with its lines. A line is shaped exactly as a single read of it would be — the same renderer, the same capabilities — and the claim is visible to its owner, a manager of its project, and expenses:view-all, expenses:approve and expenses:manage; anyone else gets the bare 404 an unknown id gets. */
        ExpensesClaimResponse: {
            abroad: boolean;
            /** @description The currency the claim's own day rate is in. Present only on a claim abroad. */
            abroadCurrency?: string;
            /**
             * Format: double
             * @description The day rate a trip abroad is paid at, in place of the dated table's. Present only on a claim abroad.
             */
            abroadDayRate?: number;
            /** @description What the claim's lines bill their customer, one line per currency. Present only for a caller with financial rights on the claim's project, exactly as the billing object on a line is. */
            billableTotals?: components["schemas"]["ExpensesCurrencyAmount"][];
            capabilities: components["schemas"]["ExpensesClaimCapabilities"];
            /** Format: date-time */
            createdAt: string;
            decision?: components["schemas"]["ExpensesEntryDecision"];
            /** Format: date-time */
            departureAt: string;
            destination?: string;
            /** Format: int64 */
            id: number;
            /** @description The claim's expenses, oldest day first and then as recorded. Always present, empty when it holds none. */
            lines: components["schemas"]["ExpensesEntryResponse"][];
            owner: components["schemas"]["ExpensesUserRef"];
            project?: components["schemas"]["ExpensesEntryProject"];
            purpose: string;
            /** @description That the employee has been paid back for the trip. Absent until then. */
            reimbursement?: components["schemas"]["ExpensesEntryReimbursement"];
            /** Format: date-time */
            returnAt: string;
            /**
             * Format: int32
             * @description What an update must carry to be allowed to save.
             */
            revision: number;
            /** @description 'draft', 'submitted', 'approved' or 'rejected'. */
            status: string;
            /** Format: date-time */
            submittedAt?: string;
            /** @description The claim's figures, one line per currency, by currency code. Nothing is ever converted. */
            totals: components["schemas"]["ExpensesCurrencyTotal"][];
            /** Format: date-time */
            updatedAt: string;
        };
        /** @description One travel claim as a *unit* in a queue — the approval queue and the reimbursement list. It is the trip at a glance, with the figures whoever is deciding needs and nothing else: the lines themselves are one read away at GET /claims/{id}, which a queue of a hundred trips has no business carrying. */
        ExpensesClaimSummary: {
            capabilities: components["schemas"]["ExpensesClaimCapabilities"];
            /** Format: date-time */
            departureAt: string;
            destination?: string;
            /** Format: int64 */
            id: number;
            /**
             * Format: int32
             * @description How many expenses the trip holds.
             */
            lineCount: number;
            /**
             * Format: int32
             * @description How many of its lines carry a rate an approver has already replaced.
             */
            overriddenRates: number;
            project?: components["schemas"]["ExpensesEntryProject"];
            purpose: string;
            /**
             * Format: int32
             * @description How many of its lines are outlays with no receipt at all. Mileage and a per diem day never take one and are never counted.
             */
            receiptsMissing: number;
            /** @description The payroll run that paid the trip back, absent until one has. Same shape and same shaping as on the claim itself — the reference is the clerk's record of their own run, so it reaches the person it paid and whoever reads everybody's expenses, and never a project manager as such. */
            reimbursement?: components["schemas"]["ExpensesEntryReimbursement"];
            /** Format: date-time */
            returnAt: string;
            /** @description The trip's own status — 'draft', 'submitted', 'approved' or 'rejected'. It is the status of every line it holds, which carries none of its own, and a queue row shows it rather than inferring it from the list it arrived in. */
            status: string;
            /** @description The trip's figures, one line per currency, by currency code. Nothing is ever converted. */
            totals: components["schemas"]["ExpensesCurrencyTotal"][];
        };
        /** @description A full replace of a travel claim's header, guarded by the revision it was read at. What is left out is cleared. Changing the project re-points every line onto it in the same transaction and clears a line's billing line when it does not belong to the new project; clearing the project makes every line non-billable. A line that has already been invoiced holds the project where it is. The owner stays whoever the claim already concerns. */
        ExpensesClaimUpdateRequest: {
            abroad?: boolean;
            abroadCurrency?: string;
            /** Format: double */
            abroadDayRate?: number;
            /** Format: date-time */
            departureAt: string;
            destination?: string;
            /**
             * Format: int32
             * @description A project the claim already carries is kept even once it stops accepting new bookings; a different one is judged in full.
             */
            projectId?: number;
            purpose: string;
            /** Format: date-time */
            returnAt: string;
            /**
             * Format: int32
             * @description The revision the claim was read at. A revision that has moved on is a 409.
             */
            revision: number;
        };
        /** @description One currency's amount in a dashboard reading. Nothing is ever converted (design §4), so a caller owed money in two currencies gets two lines and never a sum that is in neither. */
        ExpensesCurrencyAmount: {
            /** Format: double */
            amount: number;
            currency: string;
        };
        /** @description One currency's figures inside a group of expenses — the approval queue's, and the reimbursement list's. Nothing is ever converted. */
        ExpensesCurrencyTotal: {
            currency: string;
            /**
             * Format: double
             * @description The entries' gross amounts together.
             */
            gross: number;
            /**
             * Format: double
             * @description What the person is owed back — the company's own outlays add nothing.
             */
            owedToEmployee: number;
        };
        /** @description What an expense bills its customer (decision X7). Present only for a caller with financial rights on the entry's project — its managers, projects:manage-all, and projects:view-financials on a project they can see — and then always present, even with nothing in it, so a client can tell "may see, nothing billed" from "may not see". The entry's owner does not see it as such. */
        ExpensesEntryBilling: {
            /**
             * Format: double
             * @description What the customer is billed; zero on a line nobody bills. It always follows the line as it now stands — a save that changes the net or the distance works it out again from the markup or the customer rate the line carries, in an installation with no projects module too, where those two are carried through untouched.
             */
            billAmount: number;
            /**
             * Format: double
             * @description Billable mileage's rate per kilometre. Absent otherwise.
             */
            billRatePerKm?: number;
            /** @description What the customer has been billed for this line (decision X5). Absent until somebody marks it invoiced. It sits inside billing on purpose — an invoice number is the project's business, not the employee's. */
            invoice?: components["schemas"]["ExpensesEntryInvoice"];
            /**
             * Format: double
             * @description A billable outlay's or supplier invoice's markup on the net. Absent otherwise.
             */
            markupPercent?: number;
        };
        /** @description The billing line an expense is booked on, resolved through the project directory. Absent when the entry is on none, or when the directory no longer lists it. */
        ExpensesEntryBillingLine: {
            code: string;
            /** Format: int32 */
            id: number;
        };
        /** @description What the calling user may do with this entry, so a client never re-derives the rules of design §5. */
        ExpensesEntryCapabilities: {
            /** @description Whether the caller may approve or reject it — an approver of it, while it is submitted. */
            canApprove: boolean;
            /** @description Whether the caller may delete it. */
            canDelete: boolean;
            /** @description Whether the caller may change it — its owner or expenses:manage, while it is a draft or rejected, and not before the period lock. */
            canEdit: boolean;
            /** @description Whether the caller may mark it invoiced — financial rights on its project, while it is an approved billable line that carries an amount to bill and is not invoiced yet. The period lock does not hold it back: the lock protects what the employee submitted and what was approved, and invoicing is bookkeeping done after the period closes. */
            canMarkInvoiced: boolean;
            /** @description Whether the caller may mark it reimbursed — expenses:manage, while it is approved, owes the employee something and has not been paid yet. The period lock does not hold it back: payroll runs after the books close, and only expenses:manage, whom the lock never held back, marks a reimbursement. */
            canMarkReimbursed: boolean;
            /** @description Whether the caller may override the rate on it — an approver of it or expenses:manage, while it is a submitted mileage line. */
            canOverrideRate: boolean;
            /** @description Whether the billing object is in this copy of the entry. */
            canSeeBilling: boolean;
            /** @description Whether the caller may price it from the project's side (PUT /entries/{id}/billing) — financial rights on its project, while it has not been invoiced. The period lock does not reach it. */
            canSetBilling: boolean;
            /** @description Whether the caller may submit it — its owner or expenses:manage, while it is a draft or rejected, and not before the period lock. */
            canSubmit: boolean;
            /** @description Whether the caller may return it to a draft — an approver of it or expenses:manage, while it is approved and has been neither reimbursed nor invoiced. */
            canUnapprove: boolean;
            /** @description Whether the caller may take the invoicing back — financial rights on its project, while it stands invoiced. */
            canUndoInvoiced: boolean;
            /** @description Whether the caller may take the reimbursement back — expenses:manage, while it stands reimbursed. */
            canUndoReimbursed: boolean;
        };
        /** @description The category an outlay is booked under, with the name as it stands now. Absent on a mileage line, which carries none. */
        ExpensesEntryCategory: {
            /** Format: int32 */
            id: number;
            name: string;
        };
        /** @description That a billable line has been billed on to the customer, by whom and when, with the reference of the invoice it went out on. */
        ExpensesEntryInvoice: {
            /**
             * Format: date-time
             * @description When it was marked invoiced.
             */
            at: string;
            by: components["schemas"]["ExpensesUserRef"];
            /** @description The invoice it went out on, as whoever marked it typed it. Absent when none was given. */
            reference?: string;
        };
        /** @description What was decided about the expense, by whom and when — shown to everyone who may see it, its owner first of all: being told who rejected you is the point of a rejection. Present only while a decision stands; a submit and an unapprove both clear it. */
        ExpensesEntryDecision: {
            /** Format: date-time */
            at: string;
            /** @description Who decided it. Absent only for a row carrying a decision and no decider, which no operation here can produce. */
            by?: components["schemas"]["ExpensesUserRef"];
            /** @description Why it was rejected, as the approver wrote it. Absent on an approval. */
            reason?: string;
            /** @description 'approved' or 'rejected' — what was decided, which is the status the expense has been left in. */
            status: string;
        };
        /** @description The person the expense concerns — who gets the money back — which is not always who recorded it. */
        ExpensesEntryOwner: {
            /** @description Whether identity still has them as an active user. */
            active: boolean;
            /** @description Their display name; 'Unknown user' when the directory no longer knows them. */
            displayName: string;
            /** Format: uuid */
            userId: string;
        };
        /** @description The meal deductions the per diem day was priced with, as they stood in the rate table on its own date. Each one is absent rather than zero when the table held no percentage for that meal and the meal was not covered anyway: a deduction of nothing and a deduction nobody has set are different facts. A meal that *was* covered on a date the table prices no deduction for is refused instead, so a misconfigured table can never quietly pay out the whole day. */
        ExpensesEntryMealPercents: {
            /** Format: double */
            breakfast?: number;
            /** Format: double */
            dinner?: number;
            /** Format: double */
            lunch?: number;
        };
        /** @description The per diem day one line is (design §4): which kind of day it was, which meals somebody else covered, and the dated figures it was priced from. The amount itself is the line's own grossAmount — the day rate less each covered meal's percentage of it, never below zero, rounded once. */
        ExpensesEntryPerDiem: {
            breakfastCovered: boolean;
            /**
             * Format: double
             * @description What a whole day of this kind was worth — the per_diem_* rate in force on the entry date, or the claim's own abroadDayRate on a trip abroad. It is the line's rate column, so an approver's override moves it and the amount with it.
             */
            dayRate: number;
            dinnerCovered: boolean;
            lunchCovered: boolean;
            mealPercents: components["schemas"]["ExpensesEntryMealPercents"];
            /** @description 'day_6_12', 'day_over_12', 'overnight_hotel' or 'overnight_other'. */
            type: string;
        };
        /** @description The project an expense is booked on, resolved through the project directory. Absent when the entry is on none, when this installation has no projects module (decision X2), or when the directory no longer knows the project — the stored id stays either way. */
        ExpensesEntryProject: {
            code: string;
            /** Format: int32 */
            id: number;
            name: string;
        };
        /** @description The record an overridden rate leaves on a mileage line or a per diem day (decision X8) — who replaced it, and what the line was priced at before they did. Present only while an override stands: a submit reprices the line and clears it, and an unapprove clears it with the rest of the decision it undoes. Shown to everyone who may see the expense, its owner included. */
        ExpensesEntryRateOverride: {
            byUser: components["schemas"]["ExpensesUserRef"];
            /**
             * Format: double
             * @description The passenger supplement per kilometre the line was frozen at before the first override of it. Absent when the supplement was never overridden, so a client can tell a changed supplement from an untouched one.
             */
            passengerTableValue?: number;
            /**
             * Format: double
             * @description The rate the line was priced at before the first override — a mileage line's rate per kilometre, or a per diem day's day rate. It usually came from the dated rate table, but on a per diem day of a claim abroad it is the claim's own abroadDayRate, so this says "what it was priced at" rather than "what the table said". Absent when the line carried none.
             */
            tableValue?: number;
        };
        /** @description That the employee has been paid back for this expense (decision X5), by whom, when it was recorded, the day the money went and the payroll reference. Whoever may see the expense sees it — being told that you have been paid is not a privilege. */
        ExpensesEntryReimbursement: {
            /**
             * Format: date-time
             * @description When it was marked reimbursed.
             */
            at: string;
            by: components["schemas"]["ExpensesUserRef"];
            /**
             * Format: date
             * @description The day the payroll run it went with was made. A calendar date, never in the future.
             */
            date: string;
            /** @description The payroll run it went with, as whoever marked it typed it. Absent when none was given, and absent for a reader who is neither the expense's owner nor a holder of expenses:view-all, expenses:approve or expenses:manage: that the money went is everyone's business, which batch it went in is the payroll clerk's. A project manager therefore reads the stamp without this field. */
            reference?: string;
        };
        /** @description One money line (design §3.1). The fields a kind does not carry are refused on their own field rather than ignored: an outlay carries a category, a payer, a currency and a gross amount with optional VAT; a supplier invoice carries the outlay's money, a supplier, the supplier's invoice number and an optional due date, is always paid by the company, always on a project and never in a travel claim (supplier invoices design D1); a mileage line carries a distance, optional places and passengers, and is priced by the server from the dated rate table; a per diem day carries a type and three covered-meal flags, and is priced the same way. claimId records it as a line of a travel claim instead of a standalone expense, which is the only place a per diem day exists. */
        ExpensesEntryRequest: {
            /**
             * Format: double
             * @description What the customer is charged per kilometre on billable mileage. Only a caller with financial rights on the project — the ones who are sent the billing object — may name it; anyone else is refused on this field. Left out, a save keeps whatever the line already carries, and a line that carries none takes the mileage_customer rate in force on the entry date. When there is no such rate and the caller could not have named one, the line is saved billable with no customer rate and nothing billed, for whoever can see the project's money to fill in. Refused on anything but billable mileage, and on a project that bills nothing.
             */
            billRatePerKm?: number;
            /** @description Whether the line is billed on to the customer. Requires a project, and is forced false on a project that bills nothing. Refused on a per diem day, which is never billed on. */
            billable?: boolean;
            /**
             * Format: int32
             * @description An active billing line of the same project. Refused on a per diem day.
             */
            billingLineId?: number;
            /** @description Whether somebody else paid for that day's breakfast, which deducts the meal_breakfast_percent rate in force on the entry date. Per diem only; absent means false. */
            breakfastCovered?: boolean;
            /**
             * Format: int32
             * @description Required on an outlay and on a supplier invoice, and must be an active category. Refused on a mileage line and on a per diem day.
             */
            categoryId?: number;
            /**
             * Format: int64
             * @description The travel claim this expense is a line of. The caller must be allowed to change that claim, and it must still be a draft or rejected; the line takes the claim's owner and its project, so userId naming somebody else and projectId naming another project are both refused. A per diem day is refused without one: it belongs to a travel claim and exists nowhere else.
             */
            claimId?: number;
            /** @description A three-letter ISO 4217 code. Required on an outlay and on a supplier invoice. A mileage line takes the installation's default currency and refuses any other; a per diem day refuses it outright and takes the installation's currency, or the claim's own abroadCurrency on a trip abroad. */
            currency?: string;
            /** @description 1 to 500 characters. Required on an outlay, on a supplier invoice and on a mileage line. A per diem day may be left without one and then carries the empty string — what the day *is* is its perDiemType, and a name the server invented would sit in the column in one language for ever, so the client renders the label instead. */
            description?: string;
            /** @description Whether somebody else paid for that day's dinner, which deducts the meal_dinner_percent rate in force on the entry date. Per diem only; absent means false. */
            dinnerCovered?: boolean;
            /**
             * Format: double
             * @description Mileage's distance — greater than zero, at most 9999.9, at most one decimal. Refused on an outlay, a supplier invoice and a per diem day.
             */
            distanceKm?: number;
            /**
             * Format: date
             * @description When the supplier wants paying — a supplier invoice's, optional, on or after entryDate (supplier invoices design D1). Informational only; nothing here records whether the supplier has been paid. Refused on every other kind.
             */
            dueDate?: string;
            /**
             * Format: date
             * @description The day the money was spent or the distance driven — on a supplier invoice, the invoice date, the one date the period lock judges. Nothing before the period lock may be recorded except by expenses:manage. On a per diem day it is the day of the trip, which must fall between the claim's departure day and its return day, and no two per diem days of one claim may share it.
             */
            entryDate: string;
            /** @description At most 200 characters. Mileage only. */
            fromPlace?: string;
            /**
             * Format: double
             * @description An outlay's or a supplier invoice's amount including VAT — greater than zero, at most 9999999999.99, at most two decimals. Refused on a mileage line and on a per diem day, whose amounts the server computes.
             */
            grossAmount?: number;
            /** @description The supplier's own number for the invoice — required on a supplier invoice, at most 100 characters, trimmed. Free text and not unique, because there is no supplier record to make it unique under. Refused on every other kind. */
            invoiceNumber?: string;
            /** @description 'outlay', 'mileage', 'per_diem' or 'supplier_invoice'. A per diem day exists only inside a travel claim, so it is refused without a claimId. A supplier invoice is never in a travel claim, is always on a project, and is refused outright in an installation with no projects module. */
            kind: string;
            /** @description Whether somebody else paid for that day's lunch, which deducts the meal_lunch_percent rate in force on the entry date. Per diem only; absent means false. */
            lunchCovered?: boolean;
            /**
             * Format: double
             * @description A billable outlay's or supplier invoice's markup on the net, 0 to 1000 with at most two decimals. Only a caller with financial rights on the project — the ones who are sent the billing object — may name it; anyone else is refused on this field. Left out, a save keeps whatever the line already carries, and a line that carries none takes the settings' default. Refused on anything but a billable outlay or supplier invoice.
             */
            markupPercent?: number;
            /** @description 'employee' or 'company'. Required on an outlay, refused on a mileage line and on a per diem day — both are always owed to the employee. A supplier invoice is paid by the company: it may be left out or say 'company', and 'employee' is refused. */
            paidBy?: string;
            /**
             * Format: int32
             * @description 0 to 8. Mileage only; each one adds the passenger supplement per kilometre.
             */
            passengers?: number;
            /** @description 'day_6_12', 'day_over_12', 'overnight_hotel' or 'overnight_other'. Required on a per diem day and refused on every other kind. It names the per_diem_* rate the day is priced at; a type with no rate in force on the entry date is refused on this field, which is what a day of type overnight_other gets until an administrator enters the company's own rate for it. On a claim abroad the day rate is the claim's own abroadDayRate and the type is recorded rather than priced from. */
            perDiemType?: string;
            /**
             * Format: int32
             * @description The project to book the expense on. The person it concerns must be allowed to book on it — what logging time needs. A supplier invoice needs one, and it is judged instead by the recorder's financial rights on the project (the manager role, projects:manage-all, or projects:view-financials on a project they see), on any project that is not cancelled (supplier invoices design D2). Refused when this installation has no projects module.
             */
            projectId?: number;
            /** @description At most 200 characters. Optional on an outlay, required on a supplier invoice; refused on a mileage line and on a per diem day. */
            supplier?: string;
            /** @description At most 200 characters. Mileage only. */
            toPlace?: string;
            /**
             * Format: uuid
             * @description The person the expense concerns. Absent means the caller; naming somebody else needs expenses:manage, and they must be a user identity still has as active.
             */
            userId?: string;
            /**
             * Format: double
             * @description An outlay's or supplier invoice's VAT, from zero to its gross. Refused on a mileage line and on a per diem day, neither of which carries VAT.
             */
            vatAmount?: number;
        };
        /** @description One expense as the caller may see it (design §5). What the caller may not see is absent rather than null or zero — above all the billing object, which needs financial rights on the entry's project. */
        ExpensesEntryResponse: {
            /**
             * Format: int32
             * @description How many receipts the entry carries — the length of attachments, kept as its own field so a client can show the count without reading the list.
             */
            attachmentCount: number;
            /** @description The entry's receipts, oldest first. Always present, empty when it carries none, on a single read and in a list alike: the count and the list never disagree. */
            attachments: components["schemas"]["ExpensesAttachmentResponse"][];
            billable: boolean;
            billing?: components["schemas"]["ExpensesEntryBilling"];
            billingLine?: components["schemas"]["ExpensesEntryBillingLine"];
            capabilities: components["schemas"]["ExpensesEntryCapabilities"];
            category?: components["schemas"]["ExpensesEntryCategory"];
            /**
             * Format: int64
             * @description The travel claim this expense is a line of. Absent on a standalone expense. A line's status, its decision and its reimbursement are the claim's, not its own: they are rendered here from the claim, and the flow operations refuse a line by id and point at the claim.
             */
            claimId?: number;
            /** Format: date-time */
            createdAt: string;
            currency: string;
            decision?: components["schemas"]["ExpensesEntryDecision"];
            description: string;
            /**
             * Format: double
             * @description Mileage's distance. Absent on an outlay.
             */
            distanceKm?: number;
            /**
             * Format: date
             * @description A supplier invoice's due date. Absent on every other kind, and on a supplier invoice that carries none.
             */
            dueDate?: string;
            /** Format: date */
            entryDate: string;
            fromPlace?: string;
            /**
             * Format: double
             * @description An outlay's amount as entered, VAT included; a mileage line's or a per diem day's computed amount.
             */
            grossAmount: number;
            /** @description A supplier invoice's number, as the supplier wrote it. Absent on every other kind. Not the outgoing invoice stamp, which is billing.invoice. */
            invoiceNumber?: string;
            /** Format: int64 */
            id: number;
            kind: string;
            /**
             * Format: double
             * @description The gross less the VAT — the project's cost and the markup's base.
             */
            netAmount: number;
            /**
             * Format: double
             * @description What the owner gets back — the gross of an outlay they paid, a mileage line's amount, a per diem day's amount, and nothing at all for an outlay the company paid or for a supplier invoice, which the company pays.
             */
            owedToEmployee: number;
            owner: components["schemas"]["ExpensesEntryOwner"];
            paidBy?: string;
            /**
             * Format: double
             * @description The passenger supplement per kilometre the line was priced with. Absent when it carries no passengers.
             */
            passengerRate?: number;
            /** Format: int32 */
            passengers?: number;
            /** @description The per diem day this line is. Absent on every other kind. */
            perDiem?: components["schemas"]["ExpensesEntryPerDiem"];
            project?: components["schemas"]["ExpensesEntryProject"];
            /**
             * Format: double
             * @description The reimbursement rate per kilometre the line was priced with, or a per diem day's own day rate. Absent on an outlay.
             */
            rate?: number;
            rateOverride?: components["schemas"]["ExpensesEntryRateOverride"];
            /** @description That the employee has been paid back for it. Absent until then. */
            reimbursement?: components["schemas"]["ExpensesEntryReimbursement"];
            /**
             * Format: int32
             * @description What an update must carry to be allowed to save.
             */
            revision: number;
            /** @description 'draft', 'submitted', 'approved' or 'rejected'. */
            status: string;
            /** Format: date-time */
            submittedAt?: string;
            supplier?: string;
            toPlace?: string;
            /** Format: date-time */
            updatedAt: string;
            /**
             * Format: double
             * @description Absent when none was entered, rather than zero.
             */
            vatAmount?: number;
        };
        /** @description A full replace of an expense, held to every rule a create is held to, and guarded by the revision the entry was read at. What is left out is cleared — with three deliberate exceptions, because a replace only replaces what its caller can see: the markup and the customer rate per kilometre are kept unless the caller has financial rights on the project and sends one, the project a line already carries is not judged for bookability again, and in an installation with no projects module the stored project link, markup, customer rate and bill amount are carried through untouched. The owner stays whoever the entry already concerns. */
        ExpensesEntryUpdateRequest: {
            /**
             * Format: double
             * @description See the create request: only a caller with financial rights on the project may name it, and leaving it out keeps what the line already carries.
             */
            billRatePerKm?: number;
            billable?: boolean;
            /** Format: int32 */
            billingLineId?: number;
            /** @description See the create request: per diem only, absent means false. */
            breakfastCovered?: boolean;
            /** Format: int32 */
            categoryId?: number;
            /**
             * Format: int64
             * @description The travel claim the expense is already a line of. It must be exactly the one it carries, or absent: a standalone expense cannot be moved into a claim, and a claim's line cannot be moved out of one or into another.
             */
            claimId?: number;
            currency?: string;
            /** @description See the create request: required on an outlay and on a mileage line, and the empty string on a per diem day that leaves it out. */
            description?: string;
            /** @description See the create request: per diem only, absent means false. */
            dinnerCovered?: boolean;
            /** Format: double */
            distanceKm?: number;
            /**
             * Format: date
             * @description See the create request: a supplier invoice's, on or after entryDate. Left out, it is cleared.
             */
            dueDate?: string;
            /**
             * Format: date
             * @description Neither the day the entry had nor the day it is given may fall before the period lock, except for expenses:manage.
             */
            entryDate: string;
            fromPlace?: string;
            /** Format: double */
            grossAmount?: number;
            /** @description See the create request: required on a supplier invoice, refused on every other kind. */
            invoiceNumber?: string;
            /** @description Changing an outlay or a supplier invoice that still carries attachments into a mileage line or a per diem day is refused on this field: only those two kinds carry documents, so the change would leave them where nothing can reach them. Remove them first. A draft may change between outlay and supplier invoice — both carry documents, so nothing strands — and the refusals say what the new kind needs. */
            kind: string;
            /** @description See the create request: per diem only, absent means false. */
            lunchCovered?: boolean;
            /**
             * Format: double
             * @description See the create request: only a caller with financial rights on the project may name it, and leaving it out keeps what the line already carries.
             */
            markupPercent?: number;
            paidBy?: string;
            /** Format: int32 */
            passengers?: number;
            /** @description See the create request: required on a per diem day and refused on every other kind. */
            perDiemType?: string;
            /**
             * Format: int32
             * @description A project the line already carries is kept even once it stops accepting new bookings; a different one is judged in full.
             */
            projectId?: number;
            /**
             * Format: int32
             * @description The revision the entry was read at. A revision that has moved on is a 409.
             */
            revision: number;
            supplier?: string;
            toPlace?: string;
            /** Format: double */
            vatAmount?: number;
        };
        /** @description The units to move through the flow (decision X4): standalone expenses, travel claims, or both in one batch. All or nothing — one unit that may not be moved refuses the whole request, with a message per offending id on the list that named it, and nothing changes. At least one id between the two lists is required. At most 500 distinct *units* across both; an id given twice is one unit, so the cap is counted after the duplicates are collapsed. */
        ExpensesFlowRequest: {
            /** @description The travel claims to move. A claim moves as one unit — its lines go with it and are never named on their own — so an entryIds naming one of a claim's lines is refused and points at the claim. */
            claimIds?: number[];
            /** @description The standalone expenses to move. An id that belongs to a travel claim is refused with a message naming the claim. */
            entryIds?: number[];
        };
        /** @description What one batch moved. The two lists are separate because the two units are — a claim is not an expense, and a client that ticked both gets each back in the shape it reads it in — and each holds its own ids in the order they were given. */
        ExpensesFlowResponse: {
            /** @description The travel claims that moved, in the order their ids were given. */
            claims: components["schemas"]["ExpensesClaimListResponse"][];
            /** @description The standalone expenses that moved, in the order their ids were given. */
            entries: components["schemas"]["ExpensesEntryResponse"][];
        };
        /** @description Marks one approved, billable line as billed on to the customer (decision X5). The caller needs financial rights on the line's project; the period lock does not hold it back, because invoicing is bookkeeping done after the period closes. */
        ExpensesInvoicedRequest: {
            /** @description The invoice it went out on. Optional, at most 100 characters once trimmed. */
            reference?: string;
            /**
             * Format: int32
             * @description The revision the entry was read at. A revision that has moved on is a 409.
             */
            revision: number;
        };
        /** @description Takes the invoicing back off one line, under exactly the rights that set it. */
        ExpensesInvoicedUndoRequest: {
            /**
             * Format: int32
             * @description The revision the entry was read at. A revision that has moved on is a 409.
             */
            revision: number;
        };
        /** @description What the calling user may do in this module, so the frontend never re-derives the rules. */
        ExpensesMetaCapabilities: {
            /** @description Whether the caller holds expenses:approve. */
            canApprove: boolean;
            /** @description Whether the caller holds expenses:manage — settings, rates, categories, recording for a colleague, reimbursements and working past the period lock. */
            canManage: boolean;
            /** @description Whether the caller holds expenses:view-all. */
            canViewAll: boolean;
        };
        /** @description What the Expenses app needs before it can draw anything — above all whether this installation has projects at all (decision X2). Without projects there is no project field, billing line, billable flag or markup anywhere, and every request carrying one is refused on that field. */
        ExpensesMetaResponse: {
            capabilities: components["schemas"]["ExpensesMetaCapabilities"];
            /** @description Every category, active first is not assumed — they come in position order, then by name, inactive ones included so a stored expense can still name its own. */
            categories: components["schemas"]["ExpensesCategoryResponse"][];
            /** @description The currency a new expense starts in. */
            defaultCurrency: string;
            /**
             * Format: double
             * @description The markup a new billable outlay or supplier invoice starts with. Present only for expenses:manage, as on GET /settings — the server applies the default itself, so no form needs the number.
             */
            defaultMarkupPercent?: number;
            /**
             * Format: date
             * @description The period lock — nothing dated before it may be touched except by expenses:manage. Absent when no lock is set.
             */
            lockedBefore?: string;
            /** @description Whether the projects module is enabled in this installation (decision X2). */
            projectsAvailable: boolean;
            /**
             * Format: double
             * @description An employee-paid outlay above this gross amount cannot be submitted without a receipt. Zero means every one of them needs a receipt. Absent when the rule is off.
             */
            receiptRequiredOver?: number;
            /** @description The installation's business time zone, an IANA name ('Europe/Oslo' by default): the zone every date derived from a travel claim's departure and return instants is taken in. A form labelling a trip's days has to label them in it rather than in the browser's, or it will disagree with the server about which day a save lands on. The same value GET /settings answers, here because meta is the one read an expense form makes. */
            timeZone: string;
        };
        /** @description One day the server proposes for a trip. It is a suggestion and nothing more: nothing is written, and the client records whichever of them the traveller agrees with as ordinary per diem lines. A trip may run 366 days while a claim holds at most 200 expenses, so a client offering "record them all" has to reckon with the cap itself — the 201st create is refused on claimId and says nothing about the suggestion. */
        ExpensesPerDiemSuggestedDay: {
            /**
             * Format: double
             * @description What that day would come to with no meal covered — the day rate, rounded once. Absent with the rate, for the same reason.
             */
            amount?: number;
            /** @description The currency dayRate and amount are in — the claim's own abroadCurrency on a trip abroad, the installation's default otherwise. Present exactly when they are, and absent with them, so a client never has to infer it from the claim. */
            currency?: string;
            /**
             * Format: double
             * @description The per_diem_* rate in force on that date for that type, or the claim's own abroadDayRate on a trip abroad. Absent when the table prices no such day — which is what a day of type overnight_other gets until an administrator enters the company's own rate for it.
             */
            dayRate?: number;
            /**
             * Format: date
             * @description The day the period starts, as a calendar day in the installation's business time zone — the day the claim itself is judged in, so a suggested day never falls outside the trip the save judges it against.
             */
            entryDate: string;
            /** @description Whether the claim already holds a per diem line on that date. The suggestion is answered either way — what to do about a day already recorded is the client's decision, not the server's — and this is what lets it say so without a second read. */
            exists: boolean;
            /** @description 'day_6_12', 'day_over_12', 'overnight_hotel' or 'overnight_other'. */
            perDiemType: string;
        };
        /** @description What the trip's times cannot say. Whether the traveller slept away is a fact only they know, and it decides whether the trip is counted in 24-hour periods from the departure or as a single day. */
        ExpensesPerDiemSuggestionRequest: {
            /** @description Whether the traveller stayed the night away from home. It has no default: the two instants cannot tell, and a suggestion that guessed would answer a confident day count from nothing. A body that leaves it out is refused with a 400 naming it, which is why the schema does not require it — the server's own refusal says so, rather than a client's validator. */
            overnight?: boolean;
        };
        /** @description One project the caller may book an expense on, with the billing lines they may book it against — so the expense form needs no code of the projects module's own. */
        ExpensesProjectOption: {
            /** @description The project's active billing lines, by code. Empty when it has none. */
            billingLines: components["schemas"]["ExpensesProjectOptionBillingLine"][];
            code: string;
            /** @description The project's own currency, when it has one. */
            currency?: string;
            /** Format: int32 */
            id: number;
            name: string;
        };
        /** @description One billing line of a project an expense may be booked against. */
        ExpensesProjectOptionBillingLine: {
            code: string;
            /** Format: int32 */
            id: number;
        };
        /** @description One bucket of a project's expenses in one currency — what they cost the project and what they bill its customer. A figure is rounded once from the unrounded sum, so the three buckets must never be added together to make the total; read 'total' instead. */
        ExpensesProjectSummaryBucket: {
            /**
             * Format: double
             * @description What the billable lines in this bucket bill the customer. A billable line carrying no price adds nothing here and is counted in 'unpricedCount' instead — a missing price is not a price of nothing.
             */
            billAmount: number;
            /**
             * Format: double
             * @description What the lines cost the project — the net, the gross less the VAT, whoever paid. An outlay the company paid costs exactly what one the employee is reimbursed for does.
             */
            cost: number;
            /**
             * Format: int32
             * @description How many expense lines are in this bucket.
             */
            count: number;
        };
        /** @description What the caller may do about this project's expenses, so the tab needs no permission logic of its own. */
        ExpensesProjectSummaryCapabilities: {
            /** @description Whether the caller may book an expense on this project — projects' own CanLogTime, the rule a create is judged by (decision X9), so a "Record a cost" button can never offer what the save would refuse. It is false for somebody who may read these figures without being on the project's team. */
            canRecord: boolean;
            /** @description Whether the caller may record a supplier invoice on this project — financial rights on it, which reading the summary already requires, on a project that is not cancelled (supplier invoices design D2). A "Record a supplier invoice" button opens on it. Always answered; optional only because this schema's required list is never extended. */
            canRecordSupplierInvoice?: boolean;
        };
        /** @description One currency's figures for the project. A line carries its own currency, which may be neither its travel claim's nor its project's, so the figures are reported per currency and **nothing is ever converted**: the entry whose code is projectCurrency is the project's own, and every other entry is money spent in a currency the project is not in. The three status buckets are the **unit's** status — a travel claim's line is judged by its claim — and a **rejected** expense counts as draft, because it is back with its owner to fix and resubmit, exactly as Time buckets a rejected entry. There is no invoiced bucket: invoicing is a stamp, not a status, so an invoiced line is still an approved one. */
        ExpensesProjectSummaryCurrency: {
            approved: components["schemas"]["ExpensesProjectSummaryBucket"];
            /** @description The ISO 4217 code the lines were recorded in. */
            currency: string;
            draft: components["schemas"]["ExpensesProjectSummaryBucket"];
            /**
             * Format: double
             * @description What the invoiced lines billed. Per currency, never added across currencies.
             */
            invoicedAmount: number;
            /**
             * Format: int32
             * @description How many lines have been marked invoiced.
             */
            invoicedCount: number;
            /**
             * Format: double
             * @description What the lines waiting to be invoiced bill together. Per currency, never added across currencies.
             */
            readyAmount: number;
            /**
             * Format: int32
             * @description How many lines are **ready to invoice**: the unit is approved, the line is billable, it carries a bill amount, it has not been invoiced yet, and it is never a per diem day, which bills nobody anything. It is exactly what GET /entries?toInvoice=true lists, so the figure and the list can never disagree.
             */
            readyCount: number;
            submitted: components["schemas"]["ExpensesProjectSummaryBucket"];
            /** @description The part of the buckets and the total above that is supplier invoices (supplier invoices design D3). Absent when this currency holds none. It is a line of its own beneath the totals, never a split of them — every bucket above still counts every line. */
            supplierInvoices?: components["schemas"]["ExpensesProjectSummarySupplierInvoices"];
            total: components["schemas"]["ExpensesProjectSummaryBucket"];
            /**
             * Format: int32
             * @description How many billable lines carry no bill amount — billable mileage with no customer rate, say (per diem days are out of it, as they are out of readyCount). They are counted rather than billed as zero, so the figure is the work still to do before the project can invoice them.
             */
            unpricedCount: number;
        };
        /** @description What one project's expenses cost and bill, in sum. It is computed by the very code contracts.ProjectExpenses answers the projects module with, so the Expenses tab's figures and the Economy tab's can never drift apart. */
        ExpensesProjectSummaryResponse: {
            capabilities: components["schemas"]["ExpensesProjectSummaryCapabilities"];
            /** @description One entry per currency anything was recorded in, by code ascending. Empty when the project has no expenses at all — which is a project with nothing recorded, never a project the caller may not see. */
            currencies: components["schemas"]["ExpensesProjectSummaryCurrency"][];
            /**
             * Format: date
             * @description The entry date of the project's most recent expense, over every currency and every status. Absent when nothing has been recorded.
             */
            lastEntryDate?: string;
            /** @description The project as a booking option — its code, its name, its currency and its active billing lines — present exactly when capabilities.canRecordSupplierInvoice is true. It is what a "Record a supplier invoice" form is opened on, because the caller that button exists for is often on no project team, and GET /projects, a picker of what the caller may log time on, answers them nothing. */
            project?: components["schemas"]["ExpensesProjectOption"];
            /** @description The project's own currency — the entry of `currencies` with this code is the project's; every other entry is in another currency, never converted. Absent when the project carries none, in which case every entry is in another currency and the project has no figures of its own. It is answered here rather than left to a second read because the caller this endpoint exists for may hold no role on the project, and GET /api/v1/expenses/projects — which is a picker for what the caller may book on, not a lookup — answers them nothing. */
            projectCurrency?: string;
        };
        /** @description What a project's supplier invoices in one currency cost and bill, in the same three buckets as everything else and their total — the total rounded once from the unrounded whole, never the three added up. Each bucket is already inside the currency's own bucket of that status. */
        ExpensesProjectSummarySupplierInvoices: {
            approved: components["schemas"]["ExpensesProjectSummaryBucket"];
            draft: components["schemas"]["ExpensesProjectSummaryBucket"];
            submitted: components["schemas"]["ExpensesProjectSummaryBucket"];
            total: components["schemas"]["ExpensesProjectSummaryBucket"];
        };
        /** @description A replacement rate for one submitted mileage line or per diem day (decision X8). It reprices the line's amount and nothing else — the customer's own rate per kilometre is untouched — and records who set it and what the rate table had said. */
        ExpensesRateOverrideRequest: {
            /**
             * Format: double
             * @description The passenger supplement per kilometre, 0 or more with at most two decimals. Only on a mileage line that carries passengers; left out, the line keeps the supplement it was frozen with. Refused on a per diem day, which carries no supplement.
             */
            passengerRate?: number;
            /**
             * Format: double
             * @description The reimbursement rate per kilometre, or a per diem day's day rate — greater than zero, at most two decimals. On a per diem day the amount is worked out again from it and the meal percentages the line was saved with, so an approver correcting the day rate does not lose the deductions.
             */
            rate: number;
            /**
             * Format: int32
             * @description The revision the expense was read at. A revision that has moved on is a 409.
             */
            revision: number;
        };
        /** @description A new dated rate (design §3.4). A money kind carries a currency and a value greater than zero with at most two decimals; a percentage kind carries no currency and a value between 0 and 100. One row per kind and valid-from day. */
        ExpensesRateRequest: {
            /** @description A three-letter ISO 4217 code. Required for the money kinds, refused for the percentage kinds. */
            currency?: string;
            /** @description One of: mileage, mileage_passenger, mileage_customer, per_diem_6_12, per_diem_over_12, per_diem_overnight_hotel, per_diem_overnight_other, meal_breakfast_percent, meal_lunch_percent, meal_dinner_percent. */
            kind: string;
            /** @description Where the rate comes from, for display — "State rate" on a seeded row. Absent means the company's own. */
            source?: string;
            /** Format: date */
            validFrom: string;
            /** Format: double */
            value: number;
        };
        /** @description The rate kind to restore. Every row of that kind the product shipped is written back exactly as it shipped — restored when it was removed, and put back to its shipped value, currency and label when it was edited. The company's own rows, on their own days, are untouched, and nothing is removed. */
        ExpensesRateResetRequest: {
            kind: string;
        };
        /** @description One dated rate. The rate in force on a date is the row of that kind with the greatest validFrom on or before it. */
        ExpensesRateResponse: {
            /** @description The money kinds' three-letter ISO 4217 code. Absent on the percentage kinds. */
            currency?: string;
            /** Format: int32 */
            id: number;
            kind: string;
            /** @description Where the rate comes from, for display. Absent means the company's own. */
            source?: string;
            /** Format: date */
            validFrom: string;
            /** Format: double */
            value: number;
        };
        /** @description A full replace of a rate row's day, value, currency and source. The kind stays the row's own. */
        ExpensesRateUpdateRequest: {
            currency?: string;
            source?: string;
            /** Format: date */
            validFrom: string;
            /** Format: double */
            value: number;
        };
        /** @description The units one payroll run pays for (decision X5): standalone expenses, travel claims, or both. All or nothing, exactly as the flow's batches are — one unit that may not be paid refuses the whole request with a message per offending id on the list that named it, and nothing is stamped. At least one id between the two lists is required, and at most 500 distinct units across both. */
        ExpensesReimbursedRequest: {
            /** @description The travel claims to pay. A trip is paid as one unit, for the sum of what its lines owe its owner; a claim that owes nothing cannot be marked. */
            claimIds?: number[];
            /**
             * Format: date
             * @description The day the payroll run was made. A calendar date, today or earlier — never a date somebody means to pay on.
             */
            date: string;
            /** @description The standalone expenses to pay. An id that belongs to a travel claim is refused with a message naming the claim. */
            entryIds?: number[];
            /** @description The payroll run, for whoever has to find it again. Optional, at most 100 characters once trimmed. */
            reference?: string;
        };
        /** @description One person's approved units that are owed back to them, with the figures a payroll run is made from. A unit is a standalone expense or a whole travel claim, so the group holds two lists: a trip is paid as one. */
        ExpensesReimbursementGroup: {
            /** @description The person's approved travel claims that owe them something, the earliest departure first. Each is one unit of the payroll run. */
            claims: components["schemas"]["ExpensesClaimSummary"][];
            /** @description The person's standalone expenses, oldest day first and then as recorded. Shaped exactly as a single read of each would be for this caller. A travel claim's lines are never here — the claim is the unit. */
            entries: components["schemas"]["ExpensesEntryResponse"][];
            /** @description The group's figures, one line per currency, by currency code — the standalone expenses and the claims' lines together. Nothing is ever converted. */
            totals: components["schemas"]["ExpensesCurrencyTotal"][];
            user: components["schemas"]["ExpensesUserRef"];
        };
        /** @description The units to reject and why, under exactly the rules an approval follows. The reason is shown to their owner, who may then edit and submit again. At least one id between the two lists is required, and at most 500 distinct units across both. */
        ExpensesRejectRequest: {
            /** @description The travel claims to reject. The reason is stamped on the claim, which its owner edits and submits afresh. */
            claimIds?: number[];
            /** @description The standalone expenses to reject. An id that belongs to a travel claim is refused with a message naming the claim. */
            entryIds?: number[];
            /** @description Why they are rejected. Required; 1 to 1000 characters once trimmed. */
            reason: string;
        };
        /** @description A full replace of the installation's expense settings. Leaving lockedBefore or receiptRequiredOver out — or sending null — clears it. */
        ExpensesSettingsRequest: {
            /** @description A three-letter ISO 4217 code. */
            defaultCurrency: string;
            /**
             * Format: double
             * @description 0 to 1000, at most two decimals.
             */
            defaultMarkupPercent: number;
            /** Format: date */
            lockedBefore?: string;
            /**
             * Format: double
             * @description Zero or greater, at most two decimals. Zero means every employee-paid outlay needs a receipt; leaving it out turns the rule off.
             */
            receiptRequiredOver?: number;
            /** @description The installation's own business time zone, as an IANA name ('Europe/Oslo'). It is what every date derived from a travel claim's two instants is taken in — the day the period lock judges, the day GET /claims' from/to filter matches, the days a per diem line may fall on and the days the suggestion proposes — because a claim stores instants and the company's own calendar is what a period lock and a payroll month mean. A name neither Go nor Postgres knows is refused on this field. Leaving it out keeps the zone the installation already has. */
            timeZone?: string;
        };
        /** @description The installation's expense settings (design §3.5). Every expenses:access holder may read them — the lock and the receipt rule decide what they may record — and expenses:manage changes them. */
        ExpensesSettingsResponse: {
            defaultCurrency: string;
            /**
             * Format: double
             * @description The markup a new billable outlay or supplier invoice starts with. Present only for expenses:manage — what the company adds to a supplier cost before invoicing it on is a commercial figure (design §5). No form needs it: the server applies the default itself when a billable outlay or supplier invoice names no markup.
             */
            defaultMarkupPercent?: number;
            /**
             * Format: date
             * @description Absent when no lock is set.
             */
            lockedBefore?: string;
            /**
             * Format: double
             * @description An employee-paid outlay above this gross amount cannot be submitted without a receipt. Zero means every one of them needs a receipt. Absent when the rule is off.
             */
            receiptRequiredOver?: number;
            /** @description The installation's own business time zone, an IANA name. Every date derived from a travel claim's two instants is the calendar day of that instant here: the day the period lock judges, the day GET /claims' from/to filter matches, the days a per diem line may fall on and the days the suggestion proposes. A client showing a trip's days labels them in this zone and not in the browser's, or it will disagree with the server about which day a save lands on. Read by every expenses:access holder. */
            timeZone: string;
        };
        /**
         * @description One thing the dashboard wants a human to look at. The shape is the dashboard's, shared by every module's /stats/attention, and the host translates the sentence from `type` — the title is only the name of the thing.
         *
         *     Three types, each with its own recipients, and each counted in *units* — a standalone expense or a whole travel claim. `approvalWaiting`: one per person with submitted units the caller may approve; entityId and id are that person's user id, title is their display name, occurredAt is the oldest of their submissions and count is how many units are waiting. `expenseRejected`: one of the caller's own units that was sent back; for an expense entityId and id are the expense id and title is its description, for a travel claim they are 'claim/<id>' and title is the trip's purpose, and occurredAt is when it was decided, newest first and at most twenty of them. `reimbursementWaiting`: for holders of expenses:manage, a single item when anything at all is waiting to be paid; entityId and id are the literal string 'reimbursements', because the payroll list is what it links to rather than any one unit, occurredAt is the oldest approval among them and count is how many units.
         */
        ExpensesStatsAttentionItem: {
            /**
             * Format: int32
             * @description How many things the item stands for, when it stands for more than one. Absent on an item that is about a single expense. It is here because a count inside a server-written title could never be translated.
             */
            count?: number;
            entityId: string;
            id: string;
            /** Format: date-time */
            occurredAt: string;
            title: string;
            type: string;
        };
        /** @description One UTC calendar day of a time series. Days with nothing in them are absent, not zero — the dashboard's sparkline plots the points it is given. */
        ExpensesStatsDailyBucket: {
            /** Format: date */
            date?: string;
            /**
             * Format: double
             * @description The net amount approved that day, in the installation's default currency.
             */
            value?: number;
        };
        /** @description The caller's own key figures, for the strip above their expense list. Every status is always present, 0 included. */
        ExpensesStatsResponse: {
            /** Format: int32 */
            approved: number;
            /**
             * Format: int32
             * @description How many submitted expenses the caller may approve — the approval queue's own scope, counted in expenses; 0 for a caller who approves nothing.
             */
            awaitingMyApproval: number;
            /** Format: int32 */
            draft: number;
            /** Format: int32 */
            rejected: number;
            /** Format: int32 */
            submitted: number;
            /** @description What the caller is still owed — their own approved expenses that owe them something and have not been paid — one line per currency. Empty when there is nothing. */
            unreimbursed: components["schemas"]["ExpensesCurrencyAmount"][];
        };
        /** @description The dashboard's expenses card over one period, in the envelope every module's /stats/summary shares. Only awaitingMyApproval moves with the period; the other two are a state now, and neither can have a delta — a draft count cannot be reconstructed for a past moment, because a rejected expense becomes a draft again, and a currency-mixed amount would have no meaning as one. */
        ExpensesStatsSummaryResponse: {
            /**
             * Format: int32
             * @description How many submitted expenses the caller may approve; 0 for a caller who approves nothing.
             */
            awaitingMyApproval: number;
            /**
             * Format: int32
             * @description awaitingMyApproval against how many of the same expenses were already waiting when the period began — reconstructed from the expenses' submission and decision times, so one since unapproved or edited back to draft no longer counts in it.
             */
            awaitingMyApprovalDelta: number;
            /** Format: date-time */
            from: string;
            /**
             * Format: int32
             * @description How many of the caller's own expenses are still drafts.
             */
            myDrafts: number;
            /** @description What the caller is still owed, one line per currency. Empty when there is nothing. */
            myUnreimbursed: components["schemas"]["ExpensesCurrencyAmount"][];
            /** Format: date-time */
            to: string;
        };
        /** @description One person, named through identity — an expense's owner, the owner of an approval or reimbursement group, whoever overrode a rate, whoever decided the expense, whoever paid it back, or whoever invoiced it. */
        ExpensesUserRef: {
            /** @description Whether identity still has them as an active user. */
            active: boolean;
            /** @description Their display name; 'Unknown user' when the directory no longer knows them. */
            displayName: string;
            /** Format: uuid */
            userId: string;
        };
        PaginatedResponseOfExpensesApprovalGroup: {
            data: components["schemas"]["ExpensesApprovalGroup"][];
            pagination: components["schemas"]["PaginationMetadata"];
        };
        PaginatedResponseOfExpensesClaimListResponse: {
            data: components["schemas"]["ExpensesClaimListResponse"][];
            pagination: components["schemas"]["PaginationMetadata"];
        };
        PaginatedResponseOfExpensesEntryResponse: {
            data: components["schemas"]["ExpensesEntryResponse"][];
            pagination: components["schemas"]["PaginationMetadata"];
        };
        PaginatedResponseOfExpensesReimbursementGroup: {
            data: components["schemas"]["ExpensesReimbursementGroup"][];
            pagination: components["schemas"]["PaginationMetadata"];
        };
        PaginationMetadata: {
            hasNextPage: boolean;
            hasPreviousPage: boolean;
            /** Format: int32 */
            page: number;
            /** Format: int32 */
            pageSize: number;
            /** Format: int32 */
            totalCount: number;
            /** Format: int32 */
            totalPages: number;
        };
        ProblemDetails: {
            detail?: string | null;
            instance?: string | null;
            /** Format: int32 */
            status?: number | null;
            title?: string | null;
            type?: string | null;
        };
        AuthErrorResponse: {
            error: {
                code: string;
                fields?: {
                    [key: string]: string[];
                } | null;
                message: string;
            };
        };
        HttpValidationProblemDetails: {
            detail?: string | null;
            errors?: {
                [key: string]: string[];
            };
            instance?: string | null;
            /** Format: int32 */
            status?: number | null;
            title?: string | null;
            type?: string | null;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getExpensesApprovals: {
        parameters: {
            query?: {
                page?: number;
                pageSize?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedResponseOfExpensesApprovalGroup"];
                };
            };
            /** @description Bad Request — paging out of range. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller holds neither expenses:approve nor the manager role on any project, so there is nothing they could ever approve. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesApprove: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesFlowRequest"];
            };
        };
        responses: {
            /** @description OK — the approved expenses, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no ids at all, more than 500 distinct units, or at least one unit that may not be approved; the errors on entryIds and on claimIds name each offending id under the list that named it (a unit the caller may not see reads as not found), and nothing was approved. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller approves nothing at all; they hold neither expenses:approve nor the manager role on any project. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesAttachmentsById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "*/*": string;
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no receipt has that id, or the caller may not see the expense it is on. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Service Unavailable — the receipt store could not be read. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    deleteExpensesAttachmentsById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No Content */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Bad Request — on entryId, because the expense the receipt is on has moved past being editable or is dated before the period lock. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the expense is not the caller's to change; they are neither its owner nor a holder of expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no receipt has that id, or the caller may not see the expense it is on. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getExpensesCategories: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesCategoryResponse"][];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesCategories: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesCategoryRequest"];
            };
        };
        responses: {
            /** @description Created */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesCategoryResponse"];
                };
            };
            /** @description Bad Request — including a name another category already has, however it is cased. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    putExpensesCategoriesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesCategoryUpdateRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesCategoryResponse"];
                };
            };
            /** @description Bad Request — including a name another category already has, however it is cased. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no category has that id. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getExpensesClaims: {
        parameters: {
            query?: {
                /** @description Narrows the list to one person's claims. It never widens it: a caller sees only what they may see anyway. */
                userId?: string;
                /** @description 'draft', 'submitted', 'approved' or 'rejected'. */
                status?: string;
                /** @description The earliest departure day to include, judged in the installation's business time zone. */
                from?: string;
                /** @description The latest departure day to include, judged in the installation's business time zone. */
                to?: string;
                /** @description Narrows the list to what has been reimbursed, or to what has not. Left out, both are in it. */
                reimbursed?: boolean;
                page?: number;
                pageSize?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedResponseOfExpensesClaimListResponse"];
                };
            };
            /** @description Bad Request — paging out of range, or an unknown status. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesClaims: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesClaimRequest"];
            };
        };
        responses: {
            /** @description Created — a draft owned by the caller, or by the person userId names. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesClaimResponse"];
                };
            };
            /** @description Bad Request */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesClaimsById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesClaimResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no claim has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    putExpensesClaimsById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesClaimUpdateRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesClaimResponse"];
                };
            };
            /** @description Bad Request — a field did not pass, the claim has moved past being editable (on status) or departed before the period lock (on departureAt), a new trip window would leave a per diem day outside it (on departureAt or returnAt), or a change to what the trip pays would leave one unpriced (on abroad, abroadDayRate or abroadCurrency). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the claim is not the caller's to change; they are neither its owner nor a holder of expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no claim has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the claim has moved on from the revision supplied. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    deleteExpensesClaimsById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No Content */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Bad Request — the claim has moved past being editable (on status) or departed before the period lock (on departureAt). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the claim is not the caller's to change; they are neither its owner nor a holder of expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no claim has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    postExpensesClaimsByIdPerDiemSuggestion: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesPerDiemSuggestionRequest"];
            };
        };
        responses: {
            /** @description OK — the days in order, empty when the trip is too short to earn one. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesPerDiemSuggestedDay"][];
                };
            };
            /** @description Bad Request — on overnight, for a body that does not say whether the traveller stayed the night away. Nothing is suggested from a guess. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no claim has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getExpensesEntries: {
        parameters: {
            query?: {
                /** @description Narrows the list to one person's expenses. It never widens it: a caller sees only what they may see anyway. */
                userId?: string;
                /** @description Narrows the list to one project. */
                projectId?: number;
                /** @description Narrows the list to one travel claim's lines. A claim the caller may not see holds nothing they can see either, so this is an empty page rather than a refusal. */
                claimId?: number;
                /** @description true lists only standalone expenses, false only the lines of travel claims. Left out, the list holds both — the default is unchanged, so a client that knows nothing of claims sees exactly what it always saw. */
                standalone?: boolean;
                /** @description 'draft', 'submitted', 'approved' or 'rejected'. On a claim's line it is the claim's own status that is matched, because that is the status the line is rendered with. */
                status?: string;
                /** @description 'outlay', 'mileage', 'per_diem' or 'supplier_invoice'. */
                kind?: string;
                /** @description The earliest entry date to include. */
                from?: string;
                /** @description The latest entry date to include. */
                to?: string;
                /** @description Narrows the list to what has been reimbursed, or to what has not. Left out, both are in it. */
                reimbursed?: boolean;
                /** @description true lists only the lines **ready to invoice** — the unit is approved, the line is billable, it carries a bill amount, it has not been invoiced yet, and it is never a per diem day, which bills nobody anything — which is exactly the rule behind readyCount on GET /api/v1/expenses/projects/{projectId}/summary, so the list and the figure can never disagree. false means the same as leaving it out: no filter. true needs a projectId, because invoicing is done a project at a time, and is refused without one; a status other than 'approved' and kind=per_diem each contradict it and are refused rather than quietly answering an empty page. **true also needs financial rights on that project** — its manager, projects:manage-all, or projects:view-financials on a project they can see, the very rule the summary is gated on — because asking the question is itself a question about what a line bills: the answer tells the asker, per approved billable line, that it is priced and not yet invoiced, which is what a caller without those rights has stripped from every row. Anyone else is refused with a 403 whose body is the same whatever the cause (no projects module, no such project, no rights). It does not widen which rows come back: with or without it, a caller sees the expenses they could already see, shaped exactly as they were already shaped. */
                toInvoice?: boolean;
                page?: number;
                pageSize?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedResponseOfExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — paging out of range, an unknown status or kind, or toInvoice=true without a projectId or against a contradictory status or kind. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — also toInvoice=true without financial rights on the project named, with one body whatever the cause. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesEntries: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesEntryRequest"];
            };
        };
        responses: {
            /** @description Created — a draft owned by the caller, or by the person userId names. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesEntriesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    putExpensesEntriesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesEntryUpdateRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — a field did not pass, or the expense has moved past being editable (on status) or is dated before the period lock (on entryDate). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the expense is not the caller's to change; they are neither its owner nor a holder of expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the expense has moved on from the revision supplied. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    deleteExpensesEntriesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No Content */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Bad Request — the expense has moved past being editable (on status) or is dated before the period lock (on entryDate). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the expense is not the caller's to change; they are neither its owner nor a holder of expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    postExpensesEntriesByIdAttachments: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "multipart/form-data": {
                    /** Format: binary */
                    file: string;
                };
            };
        };
        responses: {
            /** @description Created */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesAttachmentResponse"];
                };
            };
            /** @description Bad Request — refused on 'file' for what was uploaded (missing, empty, too large, or not one of the four types), and on 'entryId' for what it was uploaded onto (a mileage line, an expense that has been submitted or approved, one dated before the period lock, or one that already carries ten receipts). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but it is not theirs to change. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Too Many Requests — at most 600 receipt uploads an hour from one client address, which a whole office behind one address shares. */
            429: {
                headers: {
                    "Retry-After"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Service Unavailable — the receipt store could not be written to; nothing was recorded. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    getExpensesEntriesByIdBillingLines: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesBillingLineOption"][];
                };
            };
            /** @description Bad Request — on projectId when the expense is booked on no project and so has nothing to bill a customer for, and on kind when it is a per diem day, which bills nobody anything. It refuses exactly what the save refuses, so the dialog can never offer a line the save then turns down. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but not the money its project makes on it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it, in which case the body is empty and identical to an unknown id's; with a problem body, this installation has no projects module, the same answer GET /api/v1/expenses/projects gives. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    putExpensesEntriesByIdBilling: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesBillingRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — on kind when the expense is a per diem day, which bills nobody anything, on projectId when it is on no project or this installation has no projects module, on status when it has been invoiced, and on the figures' own fields otherwise, including a markup or a customer rate named for a project that bills nothing. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but not the money its project makes on it. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the expense's revision has moved on. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    postExpensesEntriesByIdInvoiced: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesInvoicedRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — on kind when the line is a per diem day, which bills nobody anything, on projectId when it is on no project or the installation has no projects module, on status when it is not approved or is invoiced already, on billable when nobody bills it, on billAmount when it has not been priced, or on reference. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but not the project's money. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the expense has moved on since the revision that was sent. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    postExpensesEntriesByIdInvoicedUndo: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesInvoicedUndoRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — on projectId when the line is on no project or the installation has no projects module, or on status when it has not been marked invoiced. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but not the project's money. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the expense has moved on since the revision that was sent. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    putExpensesEntriesByIdRate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesRateOverrideRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesEntryResponse"];
                };
            };
            /** @description Bad Request — on kind when the expense is neither a mileage line nor a per diem day, on status when it is not submitted, on entryDate when it falls before the period lock, and on rate, passengerRate or revision otherwise. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller may see the expense but does not approve it and does not hold expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no expense has that id, or the caller may not see it. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Conflict — the expense's revision has moved on. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    getExpensesMeta: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesMetaResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesProjects: {
        parameters: {
            query?: {
                /** @description Which kind of expense the picker is for. Left out, 'outlay' or 'mileage', it is the projects the person may book on — what logging time needs. 'supplier_invoice' is instead the caller's own projects on which they hold financial rights and that are not cancelled — what recording a supplier invoice needs (supplier invoices design D2) — and cannot be combined with a userId naming somebody else, because the right to record one is the recorder's. It lists projects the caller holds a role on; a holder of projects:manage-all, or of projects:view-financials with projects:view-all, on no team records from the project page, whose summary answers the project. Any other value is refused on this field. */
                kind?: string;
                /** @description The person the expense is being recorded for. Naming anybody but the caller needs expenses:manage, the permission that lets one record for somebody else; left out, it is the caller's own projects. */
                userId?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesProjectOption"][];
                };
            };
            /** @description Bad Request — on userId, for somebody the caller may not ask about or an id nobody active has; on kind, for a kind the picker does not narrow by or supplier_invoice asked for somebody else. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — this installation has no projects module. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
        };
    };
    getExpensesProjectsByProjectIdSummary: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The project to sum up, as the projects module numbers it. */
                projectId: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK — a project with nothing recorded answers an empty currencies list, not a 404. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesProjectSummaryResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — this installation has no projects module, no project has that id, or the caller has no financial rights on it. The three are one answer with an empty body, so none of them can be told from the others. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getExpensesRates: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesRateResponse"][];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesRates: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesRateRequest"];
            };
        };
        responses: {
            /** @description Created */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesRateResponse"];
                };
            };
            /** @description Bad Request — including a validFrom the kind already has a row for. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesRatesReset: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesRateResetRequest"];
            };
        };
        responses: {
            /** @description OK — every rate as it now stands, the same list GET /rates answers. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesRateResponse"][];
                };
            };
            /** @description Bad Request — the kind is not one of the rate kinds. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    putExpensesRatesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesRateUpdateRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesRateResponse"];
                };
            };
            /** @description Bad Request — including a validFrom the kind already has another row for. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no rate has that id. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    deleteExpensesRatesById: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No Content */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Not Found — no rate has that id. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    postExpensesReimbursed: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesReimbursedRequest"];
            };
        };
        responses: {
            /** @description OK — the paid expenses, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no ids at all, more than 500 distinct units, a missing or future date, a reference over 100 characters, or at least one unit that may not be paid; the errors on entryIds and on claimIds name each offending id under the list that named it, and nothing was stamped. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesReimbursedUndo: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesFlowRequest"];
            };
        };
        responses: {
            /** @description OK — the expenses as they now stand, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no ids at all, more than 500 distinct units, or at least one unit that has not been reimbursed; the errors on entryIds and on claimIds name each offending id under the list that named it, and nothing was changed. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesReimbursements: {
        parameters: {
            query?: {
                /** @description 'waiting' (the default) or 'reimbursed'. */
                state?: string;
                /** @description Narrows the list to one person. */
                userId?: string;
                /** @description The earliest entry date to include. */
                from?: string;
                /** @description The latest entry date to include. */
                to?: string;
                page?: number;
                pageSize?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaginatedResponseOfExpensesReimbursementGroup"];
                };
            };
            /** @description Bad Request — paging out of range, or an unknown state. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesReimbursementsExportCsv: {
        parameters: {
            query?: {
                /** @description 'waiting' (the default) or 'reimbursed'. Ignored when entryIds is given. */
                state?: string;
                /** @description Narrows the export to one person. Ignored when entryIds is given. */
                userId?: string;
                /** @description The earliest entry date to include. Ignored when entryIds is given. */
                from?: string;
                /** @description The latest entry date to include. Ignored when entryIds is given. */
                to?: string;
                /** @description Exactly these standalone expenses instead of the filters, together with claimIds. An id that is not one the export could hold refuses the whole file rather than being left out of it silently, and a selection that is present but names nothing at all is refused rather than read as "everything" — a button with nothing ticked should leave both out and send the filters. One of a travel claim's lines is refused here and pointed at its claim, even when claimIds names that claim too — a payroll file holds a trip whole or not at all. */
                entryIds?: number[];
                /** @description Exactly these travel claims instead of the filters, together with entryIds. Each writes one row per line of the trip, under its own unit cell. */
                claimIds?: number[];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK — served as an attachment named expenses-reimbursements-YYYY-MM-DD.csv, and never cached. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/csv": string;
                };
            };
            /** @description Bad Request — on entryIds or claimIds for an id the export cannot hold, for more than 5000 of them, or for a selection that is present and names nothing; and, with no errors object at all, for an unknown state (the very body GET /reimbursements answers it with) or because the filters would produce more rows than one file may hold. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesReject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesRejectRequest"];
            };
        };
        responses: {
            /** @description OK — the rejected expenses, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no reason or one too long, no ids at all, more than 500 distinct units, or at least one unit that may not be rejected; the errors on entryIds and on claimIds name each offending id under the list that named it, and nothing was rejected. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller approves nothing at all. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesSettings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesSettingsResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    putExpensesSettings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesSettingsRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesSettingsResponse"];
                };
            };
            /** @description Bad Request — the currency, the markup, the receipt threshold or the time zone did not pass. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesStats: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesStatsResponse"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesStatsAttention: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesStatsAttentionItem"][];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesStatsSummary: {
        parameters: {
            query?: {
                from?: string;
                to?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesStatsSummaryResponse"];
                };
            };
            /** @description Bad Request */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    getExpensesStatsTimeseries: {
        parameters: {
            query?: {
                metric?: string;
                from?: string;
                to?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesStatsDailyBucket"][];
                };
            };
            /** @description Bad Request */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["ProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesSubmit: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesFlowRequest"];
            };
        };
        responses: {
            /** @description OK — the submitted expenses, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no ids at all, more than 500 distinct units, or at least one unit that may not be submitted; the errors on entryIds and on claimIds name each offending id under the list that named it (a unit the caller may not see reads as not found), and nothing was submitted. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
    postExpensesUnapprove: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ExpensesFlowRequest"];
            };
        };
        responses: {
            /** @description OK — the expenses as fresh drafts, each list in the order its own ids were given. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ExpensesFlowResponse"];
                };
            };
            /** @description Bad Request — no ids at all, more than 500 distinct units, or at least one unit that may not be unapproved; the errors on entryIds and on claimIds name each offending id under the list that named it, and nothing was changed. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["HttpValidationProblemDetails"];
                };
            };
            /** @description Unauthorized */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
            /** @description Forbidden — the caller approves nothing at all and does not hold expenses:manage. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthErrorResponse"];
                };
            };
        };
    };
}
