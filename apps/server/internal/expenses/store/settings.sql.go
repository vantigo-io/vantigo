// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: settings.sql

package store

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const getSettings = `-- name: GetSettings :one
SELECT id, locked_before, default_currency, default_markup_percent, receipt_required_over, updated_at FROM expenses.settings WHERE id = 1
`

// GetSettings reads the installation's single settings row (design §3.5). The
// migration writes it, so this always answers a row.
func (q *Queries) GetSettings(ctx context.Context) (ExpensesSetting, error) {
	row := q.db.QueryRow(ctx, getSettings)
	var i ExpensesSetting
	err := row.Scan(
		&i.ID,
		&i.LockedBefore,
		&i.DefaultCurrency,
		&i.DefaultMarkupPercent,
		&i.ReceiptRequiredOver,
		&i.UpdatedAt,
	)
	return i, err
}

const updateSettings = `-- name: UpdateSettings :one
UPDATE expenses.settings SET
    locked_before = $1,
    default_currency = $2,
    default_markup_percent = $3,
    receipt_required_over = $4,
    updated_at = $5::timestamptz
WHERE id = 1
RETURNING id, locked_before, default_currency, default_markup_percent, receipt_required_over, updated_at
`

type UpdateSettingsParams struct {
	LockedBefore         pgtype.Date
	DefaultCurrency      string
	DefaultMarkupPercent pgtype.Numeric
	ReceiptRequiredOver  pgtype.Numeric
	Now                  time.Time
}

// UpdateSettings replaces every setting at once; there is only ever the one
// row, written by the migration, so this never inserts.
func (q *Queries) UpdateSettings(ctx context.Context, arg UpdateSettingsParams) (ExpensesSetting, error) {
	row := q.db.QueryRow(ctx, updateSettings,
		arg.LockedBefore,
		arg.DefaultCurrency,
		arg.DefaultMarkupPercent,
		arg.ReceiptRequiredOver,
		arg.Now,
	)
	var i ExpensesSetting
	err := row.Scan(
		&i.ID,
		&i.LockedBefore,
		&i.DefaultCurrency,
		&i.DefaultMarkupPercent,
		&i.ReceiptRequiredOver,
		&i.UpdatedAt,
	)
	return i, err
}
