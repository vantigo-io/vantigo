// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: timeline.sql

package store

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const insertTimelineEntry = `-- name: InsertTimelineEntry :exec
INSERT INTO projects.timeline_entries (project_id, event_type, payload, actor_user_id, actor_display, occurred_at)
VALUES ($1, $2, $3, $4, $5, $6::timestamptz)
`

type InsertTimelineEntryParams struct {
	ProjectID    int32
	EventType    string
	Payload      []byte
	ActorUserID  *uuid.UUID
	ActorDisplay string
	Now          time.Time
}

// InsertTimelineEntry records one generated project event. It is always
// called inside the transaction of the change it accompanies, so a project
// that exists always has the entry that created it. actor_user_id is
// nullable for an event no signed-in user caused; actor_display never is, so
// the timeline stays readable after the account behind it is gone.
func (q *Queries) InsertTimelineEntry(ctx context.Context, arg InsertTimelineEntryParams) error {
	_, err := q.db.Exec(ctx, insertTimelineEntry,
		arg.ProjectID,
		arg.EventType,
		arg.Payload,
		arg.ActorUserID,
		arg.ActorDisplay,
		arg.Now,
	)
	return err
}
