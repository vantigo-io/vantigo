// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: customer_personal_data.sql

package store

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const customerProjectsForExport = `-- name: CustomerProjectsForExport :many
SELECT code, name, status, start_date, end_date
FROM projects.projects
WHERE customer_id = $1::integer
ORDER BY code
`

type CustomerProjectsForExportRow struct {
	Code      string
	Name      string
	Status    string
	StartDate pgtype.Date
	EndDate   pgtype.Date
}

// CustomerProjectsForExport is a private person's projects for their export
// (customers GDPR design D2, contracts.CustomerPersonalData): what was done
// for them, by code, name, status and dates. ix_projects_customer_id finds the
// rows.
func (q *Queries) CustomerProjectsForExport(ctx context.Context, customerID int32) ([]CustomerProjectsForExportRow, error) {
	rows, err := q.db.Query(ctx, customerProjectsForExport, customerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CustomerProjectsForExportRow
	for rows.Next() {
		var i CustomerProjectsForExportRow
		if err := rows.Scan(
			&i.Code,
			&i.Name,
			&i.Status,
			&i.StartDate,
			&i.EndDate,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
